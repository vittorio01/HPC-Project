## Next Implementation Ideas
- Maybe you can use a genetic algorithm to see which set of starting inital configurations parameters is the best?
- In the future you want to not only minimize but also maximize the function.
You can do that easily without rewriting all your code by minimizing -g(x).